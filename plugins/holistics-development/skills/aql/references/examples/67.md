---
id: 67
kind: bad
analytic: "Average order revenue of top 3 oldest users in each city. Revenue is price - discount (discount is applied on each item)."
problems: [nested_aggregation, top_n_per_group, multi_model_metric]
techniques: [fn:rank, fn:date_diff, subset_by_aggregate_filter, filter_by_subset, metric_on_many_side, explore_filters]
gotchas: [double_aggregating_measures]
---

## Dataset fields
```yaml
dimensions:
- field: id
  model: public_cities
- field: name
  model: public_cities
- field: name
  model: countries
- field: id
  model: countries
- field: name
  model: users
- field: id
  model: users
- field: birth_date
  model: users
- field: discount
  model: orders
- field: order_id
  model: items
- field: price
  model: items
measures:
- field: order_count
  model: ''
```

## Bad AQL
```aql
dimension users.age = date_diff('year', users.birth_date, @(today));
metric rank_by_user_age_in_each_city = rank(order: users | max(users.age) | desc(), partition: public_cities.name);
metric top_3_oldest_users_per_city =
  unique(users.id, public_cities.name)
  | filter(rank_by_user_age_in_each_city < 4);
metric order_revenue = items | sum(items.price - orders.discount);
metric avg_order_revenue = items | avg(order_revenue); // bad

explore {
  // NOTE: do not include users in the dimensions. Otherwise, the measures would be broken down by users, while we want to break down by city only
  dimensions {
    city_name: public_cities.name,
  }
  measures {
    avg_order_revenue: avg_order_revenue,
  }
  filters {
    users.id in (top_3_oldest_users_per_city | select(users.id)),
  }
  sorts {
    city_name ASC,
  }
}
```

## Why it's bad
* `items | avg(order_revenue)` is wrong. Nested aggregation requires proper grouping. In this case, it is implied that we want to calculate the average revenue **among users**. Without grouping, we would be **wrongly** calculate the average revenue **among all items**. The correct metric would be `unique(users.id) | select(v: order_revenue) | avg(v);`
