---
id: 40
kind: good
analytic: "List users who have made more than 100 orders of products whose price is greater than 10"
problems: [filter_on_aggregate]
techniques: [fn:where, explore_filters]
gotchas: []
---

## Dataset fields
```yaml
dimensions:
- field: id
  model: public_cities
- field: name
  model: public_cities
- field: name
  model: countries
- field: id
  model: countries
- field: name
  model: users
- field: id
  model: users
- field: created_at
  model: users
- field: order_id
  model: items
- field: id
  model: orders
- field: id
  model: product
- field: name
  model: product
- field: price
  model: product
measures: []
```

## AQL
```aql
metric order_count_of_products_whose_price_greater_than_10 =
  orders
  | count(orders.id)
  | where(product.price > 10);

explore {
  dimensions {
    user_id: users.id,
    user_name: users.name,
    user_created_at: users.created_at,
  }
  measures {
    order_count: order_count_of_products_whose_price_greater_than_10,
  }
  filters {
    order_count_of_products_whose_price_greater_than_10 > 100,
  }
  sorts {
    order_count DESC,
    user_id ASC,
  }
}
```
