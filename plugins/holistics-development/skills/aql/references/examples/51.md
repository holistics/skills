---
id: 51
kind: good
analytic: "List users along with their third order dates"
problems: [nth_per_group]
techniques: [fn:rank, fn:filter]
gotchas: []
---

## Dataset fields
```yaml
dimensions:
- field: name
  model: users
- field: id
  model: users
- field: birth_date
  model: users
- field: id
  model: orders
- field: created_at
  model: orders
measures:
- field: order_count
  model: ''
```

## AQL
```aql
metric order_rank_by_time_per_user = rank(
  order: orders.created_at | day() | asc(),
  partition: users.id,
);
metric third_order_date =
  orders
  | filter(order_rank_by_time_per_user == 3)
  | max(orders.created_at | day());

explore {
  dimensions {
    user_id: users.id,
    user_name: users.name,
  }
  measures {
    third_order_date: third_order_date,
  }
  sorts {
    user_name ASC,
  }
}
```
