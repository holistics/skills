---
id: 68
kind: bad
analytic: "Total and Average order revenue of top 3 oldest users in each city. Revenue is price - discount (discount is applied on each item)."
problems: [nested_aggregation, top_n_per_group, multi_model_metric]
techniques: [fn:rank, fn:date_diff, subset_by_aggregate_filter, filter_by_subset, metric_on_many_side, explore_filters, nested_aggregation]
gotchas: [extra_dim_changes_explore_grain]
---

## Dataset fields
```yaml
dimensions:
- field: id
  model: public_cities
- field: name
  model: public_cities
- field: name
  model: countries
- field: id
  model: countries
- field: name
  model: users
- field: id
  model: users
- field: birth_date
  model: users
- field: discount
  model: orders
- field: order_id
  model: items
- field: price
  model: items
measures:
- field: order_count
  model: ''
```

## Bad AQL
```aql
dimension users.age = date_diff('year', users.birth_date, @(today));
metric rank_by_user_age_in_each_city = rank(order: users | max(users.age) | desc(), partition: public_cities.name);
metric top_3_oldest_users_per_city =
  unique(users.id, public_cities.name)
  | filter(rank_by_user_age_in_each_city < 4);
metric order_revenue = items | sum(items.price - orders.discount);
metric avg_order_revenue_per_user =
  unique(users.id)
  | select(v: order_revenue)
  | avg(v);

explore {
  dimensions {
    city_name: public_cities.name,
    user_name: users.name, // bad
  }
  measures {
    total_order_revenue: order_revenue,
    avg_order_revenue_per_user: avg_order_revenue_per_user,
  }
  filters {
    users.id in (top_3_oldest_users_per_city | select(users.id)),
  }
  sorts {
    city_name ASC,
  }
}
```

## Why it's bad
* Putting `users.name` in explore.dimensions is wrong. It would cause the measures to be broken down by user, but we want to break them down by city ("in each city"). => Must exclude `users.name` from the explore.dimensions.
