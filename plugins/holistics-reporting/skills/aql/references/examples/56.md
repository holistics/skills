---
id: 56
kind: good
analytic: "List yesterday user reviews. Add a column to summary the review content."
problems: [inline_data_inference]
techniques: [ai_functions, custom_dim, explore_filters]
gotchas: []
---

## Dataset fields
```yaml
datasets:
  demo:
    database_type: snowflake
dimensions:
- field: id
  model: reviews
- field: content
  model: reviews
- field: created_at
  model: reviews
measures: []
```

## AQL
```aql
dimension reviews.summary = ai_summarize(reviews.content);
explore {
  dimensions {
    id: reviews.id,
    content: reviews.content,
    summary: reviews.summary,
    created_at: reviews.created_at,
  }
  filters {
    reviews.created_at matches @(yesterday),
  }
  sorts {
    id ASC,
  }
}
```
