---
id: 55
kind: good
analytic: "Calculate user cohort retention over month"
problems: [cohort_retention]
techniques: [dimensionalize_metric, fn:exclude_grains, fn:date_diff]
gotchas: []
---

## Dataset fields
```yaml
dimensions:
- field: name
  model: users
- field: id
  model: users
- field: birth_date
  model: users
- field: id
  model: orders
- field: created_at
  model: orders
- field: order_id
  model: items
- field: price
  model: items
measures: []
```

## AQL
```aql
dimension users.acquisition_cohort_month = min(orders.created_at | month()) | dimensionalize(users.id);
dimension orders.cohort_month_number = date_diff('month', users.acquisition_cohort_month, (orders.created_at | month()));
metric total_users = users | count(users.id);

explore {
  dimensions {
    acquisition_month: users.acquisition_cohort_month,
    cohort_month_number: orders.cohort_month_number,
  }
  measures {
    user_count: total_users,
    cohort_retention: (total_users * 1.0) / (total_users | of_all(orders.cohort_month_number, keep_filters: true))
  }
  sorts {
    acquisition_month ASC,
    cohort_month_number ASC,
  }
}
```
