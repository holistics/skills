---
id: 9
kind: good
analytic: "Count of orders made on Thursdays (assuming week-start-day is Sunday)"
problems: [date_part_filter]
techniques: [fn:date_part]
gotchas: []
---

## Dataset fields
```yaml
dimensions:
- field: id
  model: orders
- field: created_at
  model: orders
- field: name
  model: products
- field: id
  model: products
- field: created_at
  model: products
measures:
- field: cities_count
  model: ''
```

## AQL
```aql
dimension orders.created_at_dow = date_part('dayofweek', orders.created_at);
metric order_count = orders | count(orders.id);
explore {
  measures {
    order_count,
  }
  filters {
    orders.created_at_dow == 4,
  }
}
```
