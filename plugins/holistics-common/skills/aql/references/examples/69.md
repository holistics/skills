---
id: 69
kind: bad
analytic: "Average order count of top 3 cities (with highest revenue) in each country. Revenue is price - discount (discount is applied on each item)."
problems: [nested_aggregation, top_n_per_group, multi_model_metric]
techniques: [fn:rank, subset_by_aggregate_filter, filter_by_subset, metric_on_many_side, explore_filters, nested_aggregation]
gotchas: [wrong_inner_grain_in_nested_aggregation]
---

## Dataset fields
```yaml
dimensions:
- field: id
  model: public_cities
- field: name
  model: public_cities
- field: name
  model: countries
- field: id
  model: countries
- field: name
  model: users
- field: id
  model: users
- field: birth_date
  model: users
- field: discount
  model: orders
- field: order_id
  model: items
- field: price
  model: items
measures:
- field: order_count
  model: ''
```

## Bad AQL
```aql
dimension users.age = date_diff('year', users.birth_date, @(today));
metric order_revenue = items | sum(items.price - orders.discount);
metric order_count = orders | count(1);
metric rank_by_revenue_in_each_country = rank(order: order_revenue | desc(), partition: countries.name);
metric top_3_cities_per_country =
  unique(public_cities.id, countries.name)
  | filter(rank_by_revenue_in_each_country < 4);
metric avg_order_count_of_cities =
  unique(countries.name) // bad
  | select(v: order_count)
  | avg(v);

explore {
  // NOTE: do not include cities in the dimensions. We want to break down by country only
  dimensions {
    country: countries.name,
  }
  measures {
    avg_order_count_of_cities: avg_order_count_of_cities,
  }
  filters {
    public_cities.id in (top_3_cities_per_country | select(public_cities.id)),
  }
  sorts {
    country ASC,
  }
}
```

## Why it's bad
* `avg_order_count_of_cities` grouping by `countries.name` is wrong. We need to count orders by city first (inner aggregation), then calculate the average of those counts in each country **later** (outer aggregation) => Must group `order_count` by `public_cities.id`, then `avg` will be grouped/broken down by country later according to the `explore.dimensions`.
