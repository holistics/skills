---
id: 5
kind: good
analytic: "Top 10 products with highest order count in last quarter, compare their order count in the same quarter of 3 previous years"
problems: [top_n, period_compare]
techniques: [fn:relative_period, limit_sort]
gotchas: []
---

## Dataset fields
```yaml
dimensions:
- field: id
  model: orders
- field: created_at
  model: orders
- field: name
  model: products
- field: id
  model: products
- field: created_at
  model: products
measures:
- field: cities_count
  model: ''
```

## AQL
```aql
metric total_orders = orders | count(orders.id);
explore {
  dimensions {
    product_id: products.id,
    product_name: products.name,
  }
  measures {
    order_count_last_quarter: total_orders,
    order_count_last_year: total_orders | relative_period(orders.created_at, interval(-1 year)),
    order_count_2_years_ago: total_orders | relative_period(orders.created_at, interval(-2 year)),
    order_count_3_years_ago: total_orders | relative_period(orders.created_at, interval(-3 year)),
  }
  filters {
    orders.created_at matches @(last quarter),
  }
  sorts {
    order_count_last_quarter DESC,
    product_id ASC,
  }
  limit: 10
}
```
