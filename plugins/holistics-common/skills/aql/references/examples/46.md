---
id: 46
kind: good
analytic: "Average order count of top 3 oldest users in each city"
problems: [top_n_per_group, nested_aggregation]
techniques: [fn:rank, fn:date_diff, subset_by_aggregate_filter, subset_as_table_metric, filter_by_subset, nested_aggregation, re_aggregate_existing_measure, explore_filters]
gotchas: []
---

## Dataset fields
```yaml
dimensions:
- field: id
  model: public_cities
- field: name
  model: public_cities
- field: name
  model: countries
- field: id
  model: countries
- field: name
  model: users
- field: id
  model: users
- field: birth_date
  model: users
- field: discount
  model: orders
- field: order_id
  model: items
- field: price
  model: items
measures:
- field: order_count
  model: ''
```

## AQL
```aql
dimension users.age = date_diff('year', users.birth_date, @(today));
metric rank_by_user_age_in_each_city = rank(order: users | max(users.age) | desc(), partition: public_cities.name);
metric top_3_oldest_users_per_city =
  unique(users.id, public_cities.name)
  | filter(rank_by_user_age_in_each_city < 4);
metric avg_order_count_per_user =
  unique(users.id) // NOTE: calculate order count of each user
  | select(v: order_count)
  | avg(v);

explore {
  // NOTE: do not include users in the dimensions. Otherwise, the measures would be broken down by users, while we want to break down by city only
  dimensions {
    city_name: public_cities.name,
  }
  measures {
    avg_order_count_per_user: avg_order_count_per_user,
  }
  filters {
    users.id in (top_3_oldest_users_per_city | select(users.id)),
  }
  sorts {
    city_name ASC,
  }
}
```

## Learning points
* `order_count` is a measure and we want to calculate average on the measure => Need to do a nested aggregation
